print('''  
      ---Limite d'une compose---
      lim f(x) = b |
      x->a         |
                   | => lim (g ○ f)(x) = c  
      lim g(x) = c |    x -> a
      x->b         |
    
      --- Theoreme de Comparaison---
      A utiliser uniquement si la limite est infinie
      Encadrer le terme qui n'a pas de limite (souvent cos ou sin)
      Retrouver l'inegalite
      Specifier seulement l'inegalite utile (si on cherche a prouver +I ou -I)
      Conclure en utilisant le theoreme de comparaison
      
      --- Theoreme des gendarmes/---
      A utiliser uniquement si la limite est infinie
      Encadrer le terme qui n'a pas de limite (souvent cos ou sin)
      Retrouver l'inegalite
      Specifier la limite des fonction qui encadre la fonction dont on cherche la limite
      Conclure en utilisant le theoreme des gendarmes
      ''')